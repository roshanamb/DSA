# DSA

This solution has been updated to target .NET 8 (net8.0).

How to build:

```bash
# build and run
dotnet build
dotnet run --project DataStructureAndAlgorithms/DataStructureAndAlgorithms.csproj
```